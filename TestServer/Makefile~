all: both


client:
	g++ -o client client.cpp -lboost_system -pthread

server:
	g++ -o server server.cpp session.cc -lboost_system -pthread 

both:
	g++ -o client client.cpp -lboost_system -pthread
	g++ -o server server.cpp session.cc -lboost_system -pthread
clean:
	rm -f *.o client server
